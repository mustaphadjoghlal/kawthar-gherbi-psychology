# إعداد Firebase للموقع

يحتاج الموقع إلى مشروع Firebase واحد حتى تصبح لوحة `/admin` وتحرير المحتوى وطلبات الحجز متصلة ببيانات حية. مفاتيح واجهة Firebase العامة ليست أسراراً، لكن **قواعد Firestore وStorage هي التي تحمي المحتوى والطلبات**؛ لذلك ينبغي نشر القواعد المرفقة قبل فتح الموقع للعامة.

| الخطوة | الإجراء المطلوب |
| --- | --- |
| 1 | أنشئي مشروعاً في [Firebase Console](https://console.firebase.google.com/) وأضيفي تطبيق Web. |
| 2 | فعّلي **Cloud Firestore** و**Storage**، ثم انسخي إعداد Web إلى متغيرات البيئة المطابقة في `.env.example`. |
| 3 | من **Authentication → Sign-in method**، فعّلي Email/Password وأنشئي مستخدم الإدارة المصرح له بالدخول إلى `/admin`. |
| 4 | من شاشة **Firestore Database → Rules**، الصقي محتوى `firestore.rules` وانشريه. |
| 5 | من شاشة **Storage → Rules**، الصقي محتوى `storage.rules` وانشريه. |
| 6 | بعد تشغيل الموقع بالإعدادات، سجّلي الدخول إلى `/admin` واضغطي «تهيئة المحتوى». بعدها يصبح كل نص وصورة وخدمة ومقال قابلًا للتحرير من اللوحة. |

## متغيرات البيئة

أنشئي ملف `.env.local` للاختبار أو أضيفي هذه المتغيرات في إعدادات النشر. لا تضعي `.env.local` تحت إدارة Git.

```dotenv
VITE_FIREBASE_API_KEY=...
VITE_FIREBASE_AUTH_DOMAIN=...
VITE_FIREBASE_PROJECT_ID=...
VITE_FIREBASE_STORAGE_BUCKET=...
VITE_FIREBASE_MESSAGING_SENDER_ID=...
VITE_FIREBASE_APP_ID=...
```

## النشر على Vercel

ملف `vercel.json` يضبط عملية البناء وإعادة توجيه المسارات كي تعمل روابط مثل `/articles/when-to-pause` و`/admin` مباشرة. أضيفي متغيرات البيئة السابقة من لوحة إعدادات المشروع في Vercel قبل النشر، ثم أعيدي النشر. لا تستخدمي مفاتيح حساب الخدمة أو مفاتيح الإدارة في متغيرات `VITE_*`؛ فهي متاحة للمتصفح.

> بمجرد تهيئة المحتوى، تقرأ الواجهة البيانات من Firestore بتحديث فوري. من دون متغيرات Firebase ستظل واجهة المعاينة تعرض محتوى البداية فقط، ولن تسمح لوحة الإدارة بالحفظ.
